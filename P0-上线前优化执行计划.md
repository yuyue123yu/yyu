# P0 上线前优化执行计划

## ✅ 已完成

### 1. 多租户域名识别 ✅
- [x] 创建 `src/middleware.ts` - 域名识别中间件
- [x] 创建 `src/lib/tenant.ts` - 租户辅助函数
- [x] 更新 `src/app/api/public/services/route.ts` - 使用新的租户识别

**实现逻辑**：
1. 优先级 1：自定义域名 (`primary_domain`) - 例如：lawfirmabc.com
2. 优先级 2：子域名 (`subdomain`) - 例如：abc.legalmy.com  
3. 优先级 3：默认租户 - 主域名访问时使用第一个活跃租户
4. 开发环境：localhost 使用第一个活跃租户

**环境变量需求**：
```env
NEXT_PUBLIC_MAIN_DOMAIN=legalmy.com
```

---

## 🔄 进行中

### 2. 使用 Supabase 官方 SSR Auth
**状态**：准备开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 安装依赖
   ```bash
   npm install @supabase/ssr
   ```

2. [ ] 更新 `src/lib/supabase/server.ts`
   ```typescript
   import { createServerClient } from '@supabase/ssr'
   import { cookies } from 'next/headers'
   
   export async function createClient() {
     const cookieStore = await cookies()
     
     return createServerClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           get(name: string) {
             return cookieStore.get(name)?.value
           },
           set(name: string, value: string, options: any) {
             cookieStore.set({ name, value, ...options })
           },
           remove(name: string, options: any) {
             cookieStore.set({ name, value: '', ...options })
           },
         },
       }
     )
   }
   ```

3. [ ] 更新 `src/lib/supabase/client.ts`
   ```typescript
   import { createBrowserClient } from '@supabase/ssr'
   
   export function createClient() {
     return createBrowserClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
     )
   }
   ```

4. [ ] 更新 `src/middleware.ts` - 使用官方 SSR 方法

5. [ ] 删除自定义的 Cookie 写入逻辑
   - 删除 `/api/auth/callback` 路由
   - 更新登录页面逻辑

---

### 3. 增加 Rate Limit
**状态**：待开始  
**优先级**：P0 - 必须完成

**方案**：使用 Upstash Redis + Vercel Edge Config

**步骤**：
1. [ ] 注册 Upstash 账号
2. [ ] 创建 Redis 数据库
3. [ ] 安装依赖
   ```bash
   npm install @upstash/ratelimit @upstash/redis
   ```

4. [ ] 创建 Rate Limit 工具
   ```typescript
   // src/lib/rate-limit.ts
   import { Ratelimit } from '@upstash/ratelimit'
   import { Redis } from '@upstash/redis'
   
   export const loginRateLimit = new Ratelimit({
     redis: Redis.fromEnv(),
     limiter: Ratelimit.slidingWindow(5, '1 m'),
     analytics: true,
   })
   
   export const apiRateLimit = new Ratelimit({
     redis: Redis.fromEnv(),
     limiter: Ratelimit.slidingWindow(60, '1 m'),
     analytics: true,
   })
   ```

5. [ ] 应用到登录 API
   ```typescript
   // src/app/admin/login/page.tsx
   const ip = request.headers.get('x-forwarded-for') || 'unknown'
   const { success } = await loginRateLimit.limit(ip)
   
   if (!success) {
     return { error: '请求过于频繁，请稍后再试' }
   }
   ```

6. [ ] 应用到公开 API
   ```typescript
   // src/app/api/public/*/route.ts
   const ip = request.headers.get('x-forwarded-for') || 'unknown'
   const { success } = await apiRateLimit.limit(ip)
   
   if (!success) {
     return NextResponse.json(
       { error: 'Too many requests' },
       { status: 429 }
     )
   }
   ```

**环境变量**：
```env
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
```

---

### 4. 增加 CSP Header
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 更新 `next.config.js`
   ```javascript
   const nextConfig = {
     async headers() {
       return [
         {
           source: '/:path*',
           headers: [
             {
               key: 'Content-Security-Policy',
               value: [
                 "default-src 'self'",
                 "img-src 'self' https: data: blob:",
                 "script-src 'self' 'unsafe-inline' 'unsafe-eval'",
                 "style-src 'self' 'unsafe-inline'",
                 "font-src 'self' data:",
                 "connect-src 'self' https://*.supabase.co",
                 "frame-ancestors 'none'",
               ].join('; '),
             },
             {
               key: 'X-Frame-Options',
               value: 'DENY',
             },
             {
               key: 'X-Content-Type-Options',
               value: 'nosniff',
             },
             {
               key: 'Referrer-Policy',
               value: 'strict-origin-when-cross-origin',
             },
             {
               key: 'Permissions-Policy',
               value: 'camera=(), microphone=(), geolocation=()',
             },
           ],
         },
       ]
     },
   }
   ```

---

### 5. 增加 Audit Logs
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 检查 `audit_logs` 表是否存在
   ```sql
   SELECT * FROM information_schema.tables 
   WHERE table_name = 'audit_logs';
   ```

2. [ ] 如果不存在，执行迁移
   ```sql
   -- supabase/012_enhance_audit_logs.sql
   CREATE TABLE IF NOT EXISTS audit_logs (
     id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
     tenant_id UUID REFERENCES tenants(id) NOT NULL,
     user_id UUID REFERENCES auth.users(id),
     
     action TEXT NOT NULL,
     resource_type TEXT NOT NULL,
     resource_id UUID,
     
     old_data JSONB,
     new_data JSONB,
     
     ip_address TEXT,
     user_agent TEXT,
     
     created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
   );
   
   CREATE INDEX idx_audit_logs_tenant_id ON audit_logs(tenant_id);
   CREATE INDEX idx_audit_logs_user_id ON audit_logs(user_id);
   CREATE INDEX idx_audit_logs_created_at ON audit_logs(created_at DESC);
   CREATE INDEX idx_audit_logs_resource ON audit_logs(resource_type, resource_id);
   ```

3. [ ] 创建审计日志工具
   ```typescript
   // src/lib/audit-log.ts
   import { createServerClient } from '@/lib/supabase/server'
   import { getTenantId } from '@/lib/tenant'
   
   export async function createAuditLog({
     action,
     resourceType,
     resourceId,
     oldData,
     newData,
     request,
   }: {
     action: string
     resourceType: string
     resourceId?: string
     oldData?: any
     newData?: any
     request?: Request
   }) {
     const supabase = await createServerClient()
     const tenantId = await getTenantId()
     
     const { data: { user } } = await supabase.auth.getUser()
     
     await supabase.from('audit_logs').insert({
       tenant_id: tenantId,
       user_id: user?.id,
       action,
       resource_type: resourceType,
       resource_id: resourceId,
       old_data: oldData,
       new_data: newData,
       ip_address: request?.headers.get('x-forwarded-for') || null,
       user_agent: request?.headers.get('user-agent') || null,
     })
   }
   ```

4. [ ] 应用到关键操作
   - 服务创建/更新/删除
   - 品牌设置修改
   - 价格修改
   - SEO 设置修改
   - 管理员登录

---

### 6. 删除改为 Soft Delete
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 添加 `deleted_at` 字段到关键表
   ```sql
   -- supabase/013_add_soft_delete.sql
   ALTER TABLE services ADD COLUMN deleted_at TIMESTAMPTZ;
   ALTER TABLE tenant_settings ADD COLUMN deleted_at TIMESTAMPTZ;
   
   CREATE INDEX idx_services_deleted_at ON services(deleted_at);
   ```

2. [ ] 更新所有查询，添加 `WHERE deleted_at IS NULL`

3. [ ] 更新删除 API，改为设置 `deleted_at`
   ```typescript
   // 不再使用 .delete()
   await supabase
     .from('services')
     .update({ deleted_at: new Date().toISOString() })
     .eq('id', id)
   ```

---

### 7. robots.txt
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 创建 `src/app/robots.ts`
   ```typescript
   import { MetadataRoute } from 'next'
   
   export default function robots(): MetadataRoute.Robots {
     return {
       rules: {
         userAgent: '*',
         allow: '/',
         disallow: ['/admin/', '/api/'],
       },
       sitemap: 'https://legalmy.com/sitemap.xml',
     }
   }
   ```

---

### 8. sitemap.xml
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 创建 `src/app/sitemap.ts`
   ```typescript
   import { MetadataRoute } from 'next'
   import { createServerClient } from '@/lib/supabase/server'
   
   export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
     const supabase = await createServerClient()
     
     // 获取所有活跃服务
     const { data: services } = await supabase
       .from('services')
       .select('slug, updated_at')
       .eq('is_active', true)
       .is('deleted_at', null)
     
     const serviceUrls = services?.map((service) => ({
       url: `https://legalmy.com/services/${service.slug}`,
       lastModified: service.updated_at,
       changeFrequency: 'weekly' as const,
       priority: 0.8,
     })) || []
     
     return [
       {
         url: 'https://legalmy.com',
         lastModified: new Date(),
         changeFrequency: 'daily',
         priority: 1,
       },
       {
         url: 'https://legalmy.com/services',
         lastModified: new Date(),
         changeFrequency: 'daily',
         priority: 0.9,
       },
       ...serviceUrls,
     ]
   }
   ```

---

### 9. 自定义 404 页面
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 创建 `src/app/not-found.tsx`
   ```typescript
   import Link from 'next/link'
   
   export default function NotFound() {
     return (
       <div className="min-h-screen flex items-center justify-center bg-gray-50">
         <div className="text-center">
           <h1 className="text-6xl font-bold text-gray-900">404</h1>
           <p className="text-xl text-gray-600 mt-4">页面未找到</p>
           <Link 
             href="/" 
             className="mt-6 inline-block px-6 py-3 bg-primary-600 text-white rounded-lg"
           >
             返回首页
           </Link>
         </div>
       </div>
     )
   }
   ```

---

### 10. 法律合规页面
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 创建 `src/app/privacy/page.tsx` - 隐私政策
2. [ ] 创建 `src/app/terms/page.tsx` - 服务条款
3. [ ] 创建 `src/app/contact/page.tsx` - 联系我们
4. [ ] 创建 `src/app/cookies/page.tsx` - Cookie 政策

---

### 11. 数据库索引优化
**状态**：待开始  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] 执行索引优化 SQL
   ```sql
   -- supabase/014_add_performance_indexes.sql
   
   -- services 表索引
   CREATE INDEX IF NOT EXISTS idx_services_tenant_id ON services(tenant_id);
   CREATE INDEX IF NOT EXISTS idx_services_active ON services(is_active);
   CREATE INDEX IF NOT EXISTS idx_services_slug ON services(slug);
   CREATE INDEX IF NOT EXISTS idx_services_category ON services(category);
   CREATE INDEX IF NOT EXISTS idx_services_display_order ON services(display_order);
   
   -- profiles 表索引
   CREATE INDEX IF NOT EXISTS idx_profiles_tenant_id ON profiles(tenant_id);
   CREATE INDEX IF NOT EXISTS idx_profiles_email ON profiles(email);
   CREATE INDEX IF NOT EXISTS idx_profiles_user_type ON profiles(user_type);
   
   -- tenant_settings 表索引
   CREATE INDEX IF NOT EXISTS idx_tenant_settings_tenant_id ON tenant_settings(tenant_id);
   
   -- 复合索引
   CREATE INDEX IF NOT EXISTS idx_services_tenant_active 
   ON services(tenant_id, is_active, display_order);
   ```

---

### 12. 生产环境 HTTPS
**状态**：部署时配置  
**优先级**：P0 - 必须完成

**步骤**：
1. [ ] Vercel 自动提供 SSL
2. [ ] 确保 Cookie 设置 `secure: true`
3. [ ] 更新 Supabase 允许的重定向 URL

---

## 📊 进度追踪

- **已完成**: 1/12 (8%)
- **进行中**: 0/12
- **待开始**: 11/12 (92%)

---

## ⏰ 预计时间

- **多租户域名识别**: ✅ 已完成
- **Supabase SSR Auth**: 2 小时
- **Rate Limit**: 1 小时
- **CSP Header**: 30 分钟
- **Audit Logs**: 2 小时
- **Soft Delete**: 1 小时
- **robots.txt**: 15 分钟
- **sitemap.xml**: 30 分钟
- **404 页面**: 30 分钟
- **法律合规页面**: 2 小时
- **数据库索引**: 30 分钟
- **HTTPS 配置**: 30 分钟

**总计**: 约 11 小时

---

## 🎯 下一步行动

1. 继续完成 P0 优化项目
2. 每完成一项，更新此文档
3. 全部完成后，进行完整测试
4. 准备上线

---

**最后更新**: 2025-01-07
