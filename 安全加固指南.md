# 🛡️ 安全加固指南

## 📋 安全检查清单

### ✅ 已有的安全措施
您的项目已经包含了以下安全措施：

1. **HTTPS 加密** - Vercel 自动提供
2. **数据库 RLS（行级安全）** - Supabase 已配置
3. **环境变量隔离** - 敏感信息不在代码中
4. **JWT 认证** - Supabase Auth 提供
5. **密码加密** - Supabase 自动处理

### ⚠️ 需要加强的安全措施

---

## 🔐 第一层：环境变量安全

### 当前状态
```env
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx...
```

### ⚠️ 问题
`NEXT_PUBLIC_` 前缀的变量会暴露在浏览器中

### ✅ 解决方案

#### 1. 创建服务端专用环境变量
创建 `.env.local`：
```env
# 公开的（浏览器可见）
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx...

# 私密的（仅服务端）
SUPABASE_SERVICE_ROLE_KEY=eyJxxx...  # 从 Supabase 获取
SUPABASE_JWT_SECRET=xxx              # 从 Supabase 获取

# 加密密钥
ENCRYPTION_KEY=生成一个32位随机字符串
SESSION_SECRET=生成一个32位随机字符串
```

#### 2. 生成加密密钥
```bash
# 在终端运行
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## 🔒 第二层：API 路由保护

### 创建 API 中间件

创建 `src/middleware.ts`：

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  // 1. 限制 API 访问频率
  const ip = request.ip || request.headers.get('x-forwarded-for') || 'unknown';
  
  // 2. 保护 Admin 路由
  if (pathname.startsWith('/admin') && !pathname.startsWith('/admin/login')) {
    const token = request.cookies.get('auth-token');
    if (!token) {
      return NextResponse.redirect(new URL('/admin/login', request.url));
    }
  }

  // 3. 保护 Super Admin 路由
  if (pathname.startsWith('/super-admin') && !pathname.startsWith('/super-admin/login')) {
    const token = request.cookies.get('super-admin-token');
    if (!token) {
      return NextResponse.redirect(new URL('/super-admin/login', request.url));
    }
  }

  // 4. 添加安全头
  const response = NextResponse.next();
  
  // 防止点击劫持
  response.headers.set('X-Frame-Options', 'DENY');
  
  // 防止 MIME 类型嗅探
  response.headers.set('X-Content-Type-Options', 'nosniff');
  
  // XSS 保护
  response.headers.set('X-XSS-Protection', '1; mode=block');
  
  // 引用策略
  response.headers.set('Referrer-Policy', 'strict-origin-when-cross-origin');
  
  // 内容安全策略
  response.headers.set(
    'Content-Security-Policy',
    "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:;"
  );

  return response;
}

export const config = {
  matcher: [
    '/admin/:path*',
    '/super-admin/:path*',
    '/api/:path*',
  ],
};
```

---

## 🚫 第三层：防止暴力破解

### 1. 添加登录限制

创建 `src/lib/rateLimit.ts`：

```typescript
// 简单的内存限流器（生产环境建议使用 Redis）
const loginAttempts = new Map<string, { count: number; resetTime: number }>();

export function checkLoginAttempts(identifier: string): boolean {
  const now = Date.now();
  const attempts = loginAttempts.get(identifier);

  if (!attempts) {
    loginAttempts.set(identifier, { count: 1, resetTime: now + 15 * 60 * 1000 }); // 15分钟
    return true;
  }

  if (now > attempts.resetTime) {
    loginAttempts.set(identifier, { count: 1, resetTime: now + 15 * 60 * 1000 });
    return true;
  }

  if (attempts.count >= 5) {
    return false; // 超过5次尝试
  }

  attempts.count++;
  return true;
}

export function resetLoginAttempts(identifier: string): void {
  loginAttempts.delete(identifier);
}
```

### 2. 在登录 API 中使用

修改 `src/app/api/auth/login/route.ts`（示例）：

```typescript
import { checkLoginAttempts, resetLoginAttempts } from '@/lib/rateLimit';

export async function POST(request: Request) {
  const { email, password } = await request.json();

  // 检查登录尝试次数
  if (!checkLoginAttempts(email)) {
    return Response.json(
      { error: '登录尝试次数过多，请15分钟后再试' },
      { status: 429 }
    );
  }

  // 验证登录...
  const success = await verifyLogin(email, password);

  if (success) {
    resetLoginAttempts(email); // 成功后重置
    return Response.json({ success: true });
  } else {
    return Response.json({ error: '用户名或密码错误' }, { status: 401 });
  }
}
```

---

## 🔐 第四层：数据加密

### 1. 敏感数据加密

创建 `src/lib/encryption.ts`：

```typescript
import crypto from 'crypto';

const ENCRYPTION_KEY = process.env.ENCRYPTION_KEY || '';
const ALGORITHM = 'aes-256-gcm';

export function encrypt(text: string): string {
  const iv = crypto.randomBytes(16);
  const cipher = crypto.createCipheriv(
    ALGORITHM,
    Buffer.from(ENCRYPTION_KEY, 'hex'),
    iv
  );

  let encrypted = cipher.update(text, 'utf8', 'hex');
  encrypted += cipher.final('hex');

  const authTag = cipher.getAuthTag();

  return iv.toString('hex') + ':' + authTag.toString('hex') + ':' + encrypted;
}

export function decrypt(encryptedData: string): string {
  const parts = encryptedData.split(':');
  const iv = Buffer.from(parts[0], 'hex');
  const authTag = Buffer.from(parts[1], 'hex');
  const encrypted = parts[2];

  const decipher = crypto.createDecipheriv(
    ALGORITHM,
    Buffer.from(ENCRYPTION_KEY, 'hex'),
    iv
  );

  decipher.setAuthTag(authTag);

  let decrypted = decipher.update(encrypted, 'hex', 'utf8');
  decrypted += decipher.final('utf8');

  return decrypted;
}
```

### 2. 使用加密存储敏感数据

```typescript
import { encrypt, decrypt } from '@/lib/encryption';

// 存储时加密
const encryptedPhone = encrypt(phoneNumber);
await supabase.from('users').insert({ phone: encryptedPhone });

// 读取时解密
const { data } = await supabase.from('users').select('phone').single();
const phoneNumber = decrypt(data.phone);
```

---

## 🛡️ 第五层：SQL 注入防护

### ✅ 已防护
Supabase 客户端自动防止 SQL 注入

### ⚠️ 注意事项
如果使用原始 SQL 查询，务必使用参数化查询：

```typescript
// ❌ 危险
const { data } = await supabase.rpc('search_users', {
  query: userInput // 直接使用用户输入
});

// ✅ 安全
const { data } = await supabase
  .from('users')
  .select('*')
  .ilike('name', `%${userInput}%`); // Supabase 自动转义
```

---

## 🚨 第六层：XSS 防护

### 1. 输入验证

创建 `src/lib/validation.ts`：

```typescript
export function sanitizeInput(input: string): string {
  return input
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#x27;')
    .replace(/\//g, '&#x2F;');
}

export function validateEmail(email: string): boolean {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
}

export function validatePhone(phone: string): boolean {
  const phoneRegex = /^\+?[1-9]\d{1,14}$/;
  return phoneRegex.test(phone);
}
```

### 2. 在表单中使用

```typescript
import { sanitizeInput, validateEmail } from '@/lib/validation';

const handleSubmit = async (e: FormEvent) => {
  e.preventDefault();
  
  // 验证
  if (!validateEmail(email)) {
    setError('邮箱格式不正确');
    return;
  }
  
  // 清理输入
  const cleanName = sanitizeInput(name);
  const cleanMessage = sanitizeInput(message);
  
  // 提交...
};
```

---

## 🔒 第七层：CSRF 防护

### 1. 添加 CSRF Token

创建 `src/lib/csrf.ts`：

```typescript
import crypto from 'crypto';

export function generateCSRFToken(): string {
  return crypto.randomBytes(32).toString('hex');
}

export function verifyCSRFToken(token: string, storedToken: string): boolean {
  return crypto.timingSafeEqual(
    Buffer.from(token),
    Buffer.from(storedToken)
  );
}
```

### 2. 在表单中使用

```typescript
// 生成 token
const csrfToken = generateCSRFToken();
// 存储在 session 或 cookie 中

// 在表单中包含
<input type="hidden" name="csrf_token" value={csrfToken} />

// 验证
if (!verifyCSRFToken(submittedToken, storedToken)) {
  return Response.json({ error: 'Invalid CSRF token' }, { status: 403 });
}
```

---

## 📊 第八层：日志和监控

### 1. 创建安全日志

创建 `src/lib/securityLogger.ts`：

```typescript
export function logSecurityEvent(event: {
  type: 'login_attempt' | 'login_success' | 'login_failure' | 'suspicious_activity';
  userId?: string;
  ip: string;
  userAgent: string;
  details?: any;
}) {
  console.log('[SECURITY]', {
    timestamp: new Date().toISOString(),
    ...event,
  });

  // 生产环境：发送到日志服务（如 Sentry、LogRocket）
  if (process.env.NODE_ENV === 'production') {
    // 发送到日志服务
  }
}
```

### 2. 记录可疑活动

```typescript
import { logSecurityEvent } from '@/lib/securityLogger';

// 记录失败的登录尝试
logSecurityEvent({
  type: 'login_failure',
  ip: request.ip,
  userAgent: request.headers.get('user-agent'),
  details: { email },
});

// 记录可疑活动
if (suspiciousActivity) {
  logSecurityEvent({
    type: 'suspicious_activity',
    userId: user.id,
    ip: request.ip,
    userAgent: request.headers.get('user-agent'),
    details: { action: 'multiple_failed_attempts' },
  });
}
```

---

## 🔐 第九层：Supabase 安全配置

### 1. 在 Supabase Dashboard 配置

#### 启用邮箱验证
Settings → Authentication → Email Auth
- ✅ Enable email confirmations

#### 配置密码策略
Settings → Authentication → Password
- 最小长度：8
- 要求大小写字母
- 要求数字
- 要求特殊字符

#### 限制注册
Settings → Authentication
- ✅ Enable email domain restrictions（可选）
- ✅ Enable CAPTCHA（推荐）

### 2. 配置 RLS 策略

确保所有表都启用了 RLS：

```sql
-- 检查所有表的 RLS 状态
SELECT schemaname, tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';

-- 启用 RLS
ALTER TABLE public.your_table ENABLE ROW LEVEL SECURITY;
```

---

## 🚀 第十层：生产环境配置

### 1. Vercel 安全头配置

创建 `vercel.json`：

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        },
        {
          "key": "Permissions-Policy",
          "value": "camera=(), microphone=(), geolocation=()"
        }
      ]
    }
  ]
}
```

### 2. 配置 CORS

在 API 路由中：

```typescript
export async function POST(request: Request) {
  const origin = request.headers.get('origin');
  const allowedOrigins = [
    'https://yourdomain.com',
    'https://www.yourdomain.com',
  ];

  if (!origin || !allowedOrigins.includes(origin)) {
    return Response.json({ error: 'Forbidden' }, { status: 403 });
  }

  // 处理请求...
}
```

---

## ✅ 安全检查清单

### 部署前必做
- [ ] 所有环境变量已配置
- [ ] 生成了加密密钥
- [ ] 启用了 RLS 策略
- [ ] 配置了安全头
- [ ] 添加了登录限制
- [ ] 启用了 HTTPS
- [ ] 配置了 CORS

### 推荐配置
- [ ] 添加了 CSRF 保护
- [ ] 实现了数据加密
- [ ] 配置了安全日志
- [ ] 添加了输入验证
- [ ] 配置了 Supabase 安全设置

### 高级配置（可选）
- [ ] 集成 Sentry 错误监控
- [ ] 添加 CAPTCHA
- [ ] 配置 WAF（Web 应用防火墙）
- [ ] 实现 2FA（双因素认证）

---

## 🎯 快速实施方案

### 最小安全配置（30分钟）

1. **创建 middleware.ts**（10分钟）
   - 添加安全头
   - 保护 Admin 路由

2. **配置环境变量**（5分钟）
   - 生成加密密钥
   - 添加到 Vercel

3. **添加登录限制**（10分钟）
   - 创建 rateLimit.ts
   - 在登录 API 中使用

4. **配置 Supabase**（5分钟）
   - 启用邮箱验证
   - 配置密码策略

### 完整安全配置（2-3小时）

包括上述所有安全措施

---

## 📞 需要帮助？

如果您想实施这些安全措施，我可以：
1. 帮您创建具体的代码文件
2. 逐步指导实施
3. 解答任何安全相关的问题

您想从哪个安全措施开始？
